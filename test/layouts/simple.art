/*
 * D = A * B + C
 *
 * Where:
 *
 * A:    (  -> K )
 *       (       )
 *       (| M    )
 *       (V      )
 *
 * B:    (  -> N )
 *       (       )
 *       (| K    )
 *       (V      )
 *
 * C, D: (  -> N )
 *       (       )
 *       (| M    )
 *       (V      )
 */

static N = 16;
static M = 16;
static K = 16;

static alayout = AddrMode::RowMajor;
static blayout = AddrMode::RowMajor;
static clayout = AddrMode::RowMajor;
