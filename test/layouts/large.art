/*
 * D = A * B + C
 *
 * Where:
 *
 * A:    (  -> K )
 *       (       )
 *       (| M    )
 *       (V      )
 *
 * B:    (  -> N )
 *       (       )
 *       (| K    )
 *       (V      )
 *
 * C, D: (  -> N )
 *       (       )
 *       (| M    )
 *       (V      )
 */

static N = 4096;
static M = 4096;
static K = 4096;

static alayout = AddrMode::RowMayor;
static blayout = AddrMode::ColMayor;
static clayout = AddrMode::RowMayor;
